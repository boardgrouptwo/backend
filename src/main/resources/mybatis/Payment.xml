<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.khcare.spring">
    <!--  후원 내역 추가  -->
    <insert id="paymentInsert" parameterType="map">
        INSERT INTO payment (user_id, pay_amount, pay_date, pay_content)
        VALUES ( #{user_id}, #{pay_amount}, now(), #{pay_content} )
    </insert>

    <!--  결제 내역 출력  -->
    <select id="paymentList" parameterType="map" resultType="map">
        select user_id, pay_date, pay_amount, pay_content, '결제' AS out_type FROM payment
        UNION ALL
        SELECT user_id, spon_date AS 'pay_date', spon_money AS 'pay_amount', spon_content AS 'pay_content', '후원' AS out_type FROM sponsor
        <where>
            <if test="user_id != null and user_id > 0">
                user_id = #{user_id}
            </if>
        </where>
        <if test="!'전체'.equals(pay_type)">
            <choose>
                <when test="'원비'.equals(pay_type)">
                    HAVING out_type LIKE '%원비%'
                </when>
                <when test="'후원'.equals(pay_type)">
                    HAVING out_type LIKE '%후원%'
                </when>
            </choose>
        </if>
        ORDER BY pay_date desc
    </select>

    <select id="paymentListP" parameterType="map" resultType="map">
        select user_id, pay_date, pay_amount, pay_content, '결제' AS out_type FROM payment
        UNION ALL
        SELECT user_id, spon_date AS 'pay_date', spon_money AS 'pay_amount', spon_content AS 'pay_content', '후원' AS out_type FROM sponsor
        <where>
            <if test="user_id != null and user_id > 0">
                user_id = #{user_id}
            </if>
        </where>
        HAVING out_type LIKE '%원비%'
        ORDER BY pay_date desc
    </select>
    <select id="paymentListS" parameterType="map" resultType="map">
        select user_id, pay_date, pay_amount, pay_content, '결제' AS out_type FROM payment
        UNION ALL
        SELECT user_id, spon_date AS 'pay_date', spon_money AS 'pay_amount', spon_content AS 'pay_content', '후원' AS out_type FROM sponsor
        <where>
            <if test="user_id != null and user_id > 0">
                user_id = #{user_id}
            </if>
        </where>
        HAVING out_type LIKE '%후원%'
        ORDER BY pay_date desc
    </select>

    <!--  후원 내역 삭제 -->
    <delete id="paymentDelete" parameterType="int">
        DELETE FROM payment WHERE pay_no = #{pay_no}
    </delete>
</mapper>